// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://TU-USUARIO.github.io/lulushark/', // cámbialo por tu URL final
  trailingSlash: 'ignore',
});
